source_files += [
    meson.current_source_dir() + '/imgui_demo.cpp',
    meson.current_source_dir() + '/imgui_draw.cpp',
    meson.current_source_dir() + '/imgui_impl.cpp',
    meson.current_source_dir() + '/imgui_widgets.cpp',
    meson.current_source_dir() + '/imgui.cpp'
]